# Глоссарий Nowhere Fast

**Дата**: 2026-09-16
**Статус**: рабочий словарь. Часть пунктов помечена как требующая решения владельца.

## Зачем этот файл

Слово «маршрут» в проекте означает одновременно две разные вещи: авторский контент
(таблица `route_templates` — готовый маршрут без дат и без отеля) и будущий результат
планировщика (порядок обхода на конкретные даты поездки).

Слова разведены до того, как планировщик написан, — и это окупилось: 16.09.2026
появились `assets/js/planner.js`, `assets/js/trip.js` и `plan.html`, и ни одно имя
в них не пришлось выбирать заново (ADR-0007).

Правило из первоисточника (`docs/TRANSFER-FROM-WB.md`, раздел 1.7, ссылка на
`docs/SPEC/12-glossary.md:10` в wb-analytics):

> Определение без границы не работает: путаницу создаёт не отсутствие определения,
> а соседний похожий термин.

Поэтому у каждого термина здесь есть колонка «Что это НЕ». Она важнее определения.

## Термины

| Термин | Что это | Что это НЕ | Где живёт в коде/схеме |
|---|---|---|---|
| **Место** | Точка на карте с координатами, описанная один раз и переиспользуемая где угодно. Координаты обязательны (ADR-0003, ADR-0004) | Не активность. Существует вне любого плана и любого маршрута | Таблица `places` (`supabase/schema.sql`); `lat`/`lng` — `not null`; `NF.api.listPlaces` / `createPlace` / `deletePlace` в `assets/js/api.js` |
| **Активность** | Посещение места внутри одного дня одного авторского маршрута, с порядком `order_in_day` | Не место. Не носитель факта о месте: текст без координат в планировании не участвует | Таблица `route_template_activities`; ссылка `place_id` на `places` |
| **День** | Порядковый день внутри авторского маршрута, номер `day_number` | Не календарная дата. К датам поездки не привязан | Таблица `route_template_days`; уникальность `(route_template_id, day_number)` |
| **Авторский маршрут** | Готовый контент от автора: название, описание, дни, активности. Модель «писать могут все» из брифа | Не план. Без дат, без отеля, без конкретного пользователя | Таблица `route_templates` (`author_id`, `is_published`, `days_count`, `difficulty`, `price_cents`, `currency_code`) |
| **План** | Результат работы планировщика: порядок обхода мест по дням конкретной поездки, с часами прихода, предупреждениями и объяснением чисел | Не авторский маршрут. Привязан к датам и к точке старта. Не хранится нигде: он пересчитывается каждый раз из поездки | Возврат `NF.planner.planRoute(...)` (`assets/js/planner.js`): `{days, dropped, warnings, nearby, explain}`; показывает `plan.html`. Таблицы в базе нет |
| **Поездка** | Контейнер: город + даты + точка старта + отметки. То, из чего строится план | Не план. Сама по себе порядка обхода не содержит | `assets/js/trip.js`, `NF.trip` — объект `{cityId, from, to, start, selected}`. Живёт в адресе страницы и в локальном хранилище (`nf.trip.<cityId>`), **адрес старше хранилища**. Таблицы в схеме нет: `trips`, `trip_activities`, `trip_packing` удалены 16.09.2026, аккаунты вне первой версии (ADR-0002, ADR-0007) |
| **Город** | Контейнер для мест и авторских маршрутов; центр города — точка отсчёта, когда отеля ещё нет | Не страна и не регион. Одно имя в разных странах — разные города (Кембридж) | Таблица `cities` (`lat`, `lng`, `timezone`, `name_local`); уникальность по паре `(lower(name), lower(country))`, индекс `cities_name_country_key` |
| **Черновик** | Запись с `is_published = false`: видна только своему автору, посетителю не показывается | Не удалённая запись и не «неготовые данные» в смысле качества | Колонка `is_published` в `places` и `route_templates`; умолчание — `false` в обеих таблицах |
| **Публикация** | Перевод записи в `is_published = true`. Граница проводится RLS на стороне Supabase, а не интерфейсом | Не «выложить на GitHub Pages» и не деплой | Политики `places_read_published`, `route_templates_read_published` в `supabase/policies.sql` |
| **Источник данных** | Откуда пришёл факт о месте: `manual`, `map`, `author`, `ai` (ADR-0003) | Не автор записи (`author_id`) и не провайдер карт — провайдер ещё не выбран | Колонка `places.source`, ограничение `places_source_known` |
| **Планировщик** | Расчёт, который из отмеченных мест, дат и точки старта собирает план | Не админка и не каталог авторских маршрутов. Не поиск мест. Не страница: `plan.html` только показывает то, что он вернул | `assets/js/planner.js`, чистая функция `NF.planner.planRoute({places, selectedIds, dates, start, options})`: ни сети, ни `document`, ни «сейчас» — даты подаются аргументом |
| **Точка старта** | Откуда начинается день: координаты и название — отель, центр города или любое место (допущение A5) | Не место в плане: посещать её не идут, длительности у неё нет, в списке дня она не появляется. Не обязательна — без неё план строится, но приходит предупреждение `no_start` | `trip.start` = `{lat, lng, name}` (`assets/js/trip.js`, параметр `start=` в адресе); `normalizeStart` в `assets/js/planner.js`; поле «Откуда начинаю день» в `city.html` |
| **Отметка** | «Хочу сюда»: место, выбранное галочкой. Не больше `MAX_SELECTED` (60) — и от здравого смысла, и от длины ссылки | Не бронирование и не обещание попасть в план: отмеченное выпадает без координат (`dropped`) или не влезает в дни (`not_fitted`). Не план — отметки не упорядочены | `trip.selected` — массив id мест; `NF.trip.toggle` / `isSelected`; параметр `sel=` в адресе; галочка «Хочу сюда» в `city.html` |
| **Допущение** | Число или правило, подставленное нами, потому что данных нет: окно дня, длительность посещения, скорость, коэффициент крюка, порог «рядом» (A1–A6) | Не факт и не решение владельца. Пока на Q1–Q10 нет ответов, любое из них отменяется одним словом, и числа пересчитываются | `DEFAULTS` в `assets/js/planner.js`; признак `minutesAssumed` у точки дня; `explain.assumptions` и блок «На чём построен этот расчёт» в `plan.html`; список — `docs/ACCEPTANCE-PLANNER.md` |
| **Окно дня** | Границы, внутри которых планируется день: от `dayStart` до `dayEnd`, по умолчанию 09:00–21:00 (допущение A2). Что не уложилось — едет в следующий день или в «не влезло» | Не календарная дата и не часы работы места: `opening_hours` не учитывается вовсе (A4). Не гарантия: время внутри окна — оценка (A1) | `dayStartMinutes` / `dayEndMinutes`, `dayWindow()` и `packDays()` в `assets/js/planner.js`; незанятый остаток окна — `idleMinutes` дня |

## Слова, которых избегаем

**Голое «маршрут» без уточнения — запрещено.** Пишем либо «авторский маршрут»,
либо «план». Это единственное жёсткое требование раздела.

| Плохо | Хорошо |
|---|---|
| «показать маршруты города» | «показать авторские маршруты города» |
| «построить маршрут на 5 дней» | «построить план на 5 дней» |
| «маршрут пользователя» | «план поездки» |

Остальные слова под подозрением, но не запрещены, потому что замена ещё не согласована:

- **«точка»** — иногда место, иногда координата, иногда отель, а с 16.09.2026 ещё
  и «точка старта» — отдельный термин с собственным смыслом. Двусмысленности стало
  больше, не меньше. Говорим «место», «координаты» или «точка старта» целиком.
- **«день»** — в авторском маршруте это номер (`day_number`), в поездке будет дата.
  Если из контекста неясно — пиши «день маршрута» или «дата поездки».

## Известная коллизия в схеме

Слово `route` в базе уже занято авторским контентом:

- `route_templates`
- `route_template_days`
- `route_template_activities`

Переименование этих таблиц **в этот заход не делается**: в них лежат данные, на них
завязаны `assets/js/api.js`, `city.html`, `admin.html` и политики в
`supabase/policies.sql`. Цена переименования сейчас выше пользы.

**Принято (ADR-0007, 16.09.2026): новый код называется `plan` / `planner`, а не `route`.**
`route*` означает авторский контент, `plan*` — результат расчёта.

Так и сделано: `assets/js/planner.js`, `assets/js/trip.js`, `plan.html`, `NF.planner`,
`NF.trip`. Единственное исключение — имя функции `planRoute`: оно пришло из
`docs/TRANSFER-FROM-WB.md` (раздел 1.9) и осталось. Слово `route` внутри него
означает «порядок обхода», а не авторский маршрут; новых имён с `route` не заводим.

## Правило сопровождения

Запрет термина сопровождается **разовой заменой во всех документах** — в том же коммите,
что и запрет.

Иначе получается худший вариант: новое слово запрещено употреблять, а в старых местах
живёт прежнее, и читатель не понимает, какое из двух правильное.

Проверка перед коммитом, меняющим этот файл:

```
grep -rn "[Мм]аршрут" docs/ .claude/ CLAUDE.md *.html assets/
```

Шаблон с обеими буквами не для красоты: `grep -i` на кириллице зависит от локали
и в рабочей среде молча пропускает «Маршрут» с большой буквы — проверено
16.09.2026: четыре вхождения в `city.html` и три в `api.js` так и не нашлись.

Каждое вхождение — либо «авторский маршрут», либо «план», либо цитата из ADR, которую
не правим (ADR — исторический документ, его текст не переписывается задним числом).

## Что здесь ещё не решено

- Имя сущности «поездка» в будущей схеме (`trips` уже был и удалён). Сейчас поездка
  живёт только в адресе и в хранилище браузера; появится таблица — понадобится имя.
  Требует решения владельца.
- Нужна ли замена слов «точка» и «день» на что-то однозначное. С появлением
  «точки старта» вопрос стал острее. Требует решения владельца.

Приставка `plan`/`planner` из этого списка убрана: решена в ADR-0007, см. раздел выше.
